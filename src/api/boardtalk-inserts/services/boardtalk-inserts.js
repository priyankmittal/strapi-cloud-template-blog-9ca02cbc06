'use strict';

/**
 * boardtalk-inserts service
 */

const { createCoreService } = require('@strapi/strapi').factories;

module.exports = createCoreService('api::boardtalk-inserts.boardtalk-inserts');
