'use strict';

/**
 * boardtalk-inserts controller
 */

const { createCoreController } = require('@strapi/strapi').factories;

module.exports = createCoreController('api::boardtalk-inserts.boardtalk-inserts');
